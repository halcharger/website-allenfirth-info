# Local Pages Functions development only.
#   npm run build
#   npx wrangler pages dev .output/public
#
# Git Cloudflare Pages deploys skip this file unless `pages_build_output_dir` is
# set. Do NOT set that key — it makes Pages skip the dashboard build command.
#
# Resend: API key from the account for allen@codestream.co.za
# (verified domain codestream.co.za). Production/preview secrets live in the
# Pages dashboard, not here.

name = "allenfirth-info"
compatibility_date = "2026-08-26"

[vars]
CONTACT_TO = "allen@codestream.co.za"
CONTACT_FROM = "Allen Firth <noreply@codestream.co.za>"
